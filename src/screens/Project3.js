import {memo} from 'react';
import {Text, View} from 'react-native';

const Project3 = ({}) => {
  return (
    <View>
      {' '}
      <Text>Hello</Text>
    </View>
  );
};

export default memo(Project3);
